DATABASE_URL=postgres://civicpulse:civicpulse@localhost:5432/civicpulse
PORT=3001
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Deployed API only: where uploads are written, and which frontend origins may call the API.
UPLOAD_DIR=uploads
CORS_ORIGIN=
# Deployed frontend only (build time): absolute API base URL, e.g. https://civicpulse-api.onrender.com
VITE_API_URL=
